@model UserState
@{
	var serialized = JsonSerializer.Serialize(Model, new JsonSerializerOptions{ PropertyNamingPolicy = JsonNamingPolicy.CamelCase });
}
<script>
	var userState = Object.assign(new UserState(), @Html.Raw(serialized));
</script>